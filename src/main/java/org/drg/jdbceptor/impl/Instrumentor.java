package org.drg.jdbceptor.impl;

/**
 * Centralized logic for managing callbacks and invoking associated listeners, etc. This contains mostly logic that
 * would otherwise live in the Jdbceptor class itself, but is here to avoid polluting the actually Driver implementation
 * class.
 *
 * @author dgarson
 */
public class Instrumentor {
}
